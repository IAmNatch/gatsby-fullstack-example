import React from 'react';
import styled from 'styled-components';
import { DivProto } from '../Prototypes';

// Box can have custom attributes, but currently takes everything from the base.
export const Box = styled(DivProto)``;
